import { EditorState } from "@tiptap/pm/state";
declare const duplicateRow: (state: EditorState, dispatch: ((args?: any) => any) | undefined, withContent?: boolean) => boolean;
export default duplicateRow;
