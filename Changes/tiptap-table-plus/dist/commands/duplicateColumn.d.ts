import { EditorState } from '@tiptap/pm/state';
declare const duplicateColumn: (state: EditorState, dispatch: ((args?: any) => any) | undefined, withContent?: boolean) => boolean;
export default duplicateColumn;
